name: Maintain Woods Run SEO

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  seo-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify SEO maintenance script
        run: python scripts/maintain_seo.py
